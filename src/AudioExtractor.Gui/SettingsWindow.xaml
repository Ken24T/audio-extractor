<Window x:Class="AudioExtractor.Gui.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="520" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Text="ffmpeg" Margin="0,0,12,12" VerticalAlignment="Center" />
    <TextBox Grid.Row="0" Grid.Column="1" Name="FfmpegPathTextBox" Margin="0,0,12,12" />
    <Button Grid.Row="0" Grid.Column="2" Content="Browse" Width="90" Margin="0,0,0,12" Click="OnBrowseFfmpeg" />

    <TextBlock Grid.Row="1" Grid.Column="1" Text="Leave blank to use PATH." Foreground="Gray" Margin="0,0,0,12" />

    <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True" />
      <Button Content="OK" Width="90" IsDefault="True" Click="OnOk" />
    </StackPanel>
  </Grid>
</Window>
