<Window x:Class="AudioExtractor.Gui.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="480" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <StackPanel Margin="16">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0" Text="ffmpeg" Margin="0,0,12,0" VerticalAlignment="Center" />
      <TextBox Grid.Column="1" Name="FfmpegPathTextBox" Margin="0,0,8,0" />
      <Button Grid.Column="2" Content="Browse" Width="80" Click="OnBrowseFfmpeg" />
    </Grid>

    <TextBlock Text="Leave blank to use PATH." Foreground="Gray" FontSize="11" Margin="0,6,0,0" />

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True" Style="{StaticResource SecondaryButtonStyle}" />
      <Button Content="OK" Width="80" IsDefault="True" Click="OnOk" />
    </StackPanel>
  </StackPanel>
</Window>
